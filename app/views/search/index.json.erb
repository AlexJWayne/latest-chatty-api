<% if params[:callback] %>
  <%= params[:callback] %>(<%= "{\"comments\":[#{@posts.collect(&:to_json).join(',')}]}" %>)
<% else %>
  <%= "{\"comments\":[#{@posts.collect(&:to_json).join(',')}]}" %>
<% end %>